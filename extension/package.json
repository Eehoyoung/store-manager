{
  "name": "review-pilot-extension",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check-selectors": "node --disable-warning=ExperimentalWarning --experimental-strip-types scripts/check-selectors.mjs"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.7.1",
    "@types/chrome": "^0.3.0",
    "@types/node": "^22.10.0",
    "jsdom": "^27.0.1",
    "typescript": "^5.7.2",
    "vite": "^6.4.3",
    "vitest": "^5.0.1"
  }
}
